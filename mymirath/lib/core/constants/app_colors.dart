import 'package:flutter/material.dart';

class AppColors {
  // 🟢 Primary brand (Islamic identity)
  static const primary = Color(0xFF0F3D2E); // deep green

  // 🟡 Accent (buttons/highlights)
  static const accent = Color(0xFFD4AF37); // gold (heritage feel)

  // ⚪ Background (from your React #FAFAFA)
  static const background = Color(0xFFFAFAFA);

  // ⚫ Text primary
  static const textPrimary = Color(0xFF1C1C1C);

  // ⚫ Text secondary
  static const textSecondary = Color(0xFF6B6B6B);

  // 🧊 Card background
  static const card = Colors.white;

  // 🔵 Optional soft highlight
  static const softBlue = Color(0xFFE8F1FF);
  static const Color surface = Color(0xFFFAFAFA);
}